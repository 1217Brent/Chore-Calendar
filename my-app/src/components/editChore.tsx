import React from "react"

interface EditChoreProps {
    userId: string;
}

const EditChore: React.FC<EditChoreProps> = ({ userId }) => {
    //copy and paste the page from createChore and fetch the fields from it

    

    return (<div>

    </div>);
}